{% extends "MesdUserBundle::layout.html.twig" %}

{% block mesd_user_content %}
    <h1>Showing users that have {{ entity }} filter</h1>

    <table class="record_properties table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity }}</td>
            </tr>
            <tr>
                <th>Users</th>
                <td>
                    {% for user in entity.user %}{% if loop.index0 > 0 %},{% endif %}
                        {{ user }}
                    {% endfor %}
                </td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('MesdUserBundle_onefiltermanyusers') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('MesdUserBundle_onefiltermanyusers_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
</ul>
{% endblock mesd_user_content %}
